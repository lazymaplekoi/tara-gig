{
  "name": "tara-gig",
  "version": "1.0.0",
  "description": "A gig directory app",
  "devDependencies": {
    "autoprefixer": "^10.4.14",
    "postcss": "^8.4.23",
    "tailwindcss": "^3.3.2"
  },
  "scripts": {
    "start": "concurrently \"python manage.py runserver\" \"npm run watch:css\"",
    "watch:css": "npx tailwindcss-cli@latest build -o static/css/styles.css --watch"
  },
  "dependencies": {
    "concurrently": "^8.2.0"
  }
}
